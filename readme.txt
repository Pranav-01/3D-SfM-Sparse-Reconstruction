To run the code, execute the cells in the sfm.ipynb file

To plot the final 3D points, run the plotter.py file in the terminal:
python3 plotter.py

By default the plotter code plots the optimized 3D points